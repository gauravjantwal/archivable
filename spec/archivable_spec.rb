require 'spec_helper'

describe Archivable do
  it 'has a version number' do
    expect(Archivable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
